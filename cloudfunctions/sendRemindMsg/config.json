{
    "permissions": {
        "openapi": [
            "templateMessage.send",
            "subscribeMessage.send"
        ]
    },
    "triggers": [
        {
            "name": "sendRemindMsg",
            "type": "timer",
            "config": "0 * * * * * *"
        }
    ]
}